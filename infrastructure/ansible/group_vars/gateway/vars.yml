---
# Connection settings for the gateway server (used after Task 3 hardening)
ansible_port: 6969
ansible_user: "{{ new_user }}"
ansible_password: "{{ vault_ssh_password }}"
ansible_become_password: "{{ vault_sudo_password }}"
prometheus_auth_password: "{{ vault_prometheus_auth_password }}"
cloudflare_api_token: "{{ vault_cloudflare_api_token }}"
